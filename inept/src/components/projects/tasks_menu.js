import './tasks_menu.css';
import React, { useState} from 'react';
function TasksMenu({id, projectBulk}) {
    return(
                <div>
                    {"Active Project is "+id}
                </div>
    );
  }
  
  export default TasksMenu;